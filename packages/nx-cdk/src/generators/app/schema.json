{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "NxCdk",
  "title": "app",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the frontend project to test.",
      "$default": {
        "$source": "projectName"
      }
    },
    "name": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    },
    "cdkCommand": {
      "type": "string",
      "description": "Command used to invoke cdk",
      "default": "npx cdk",
      "x-prompt": "What command do you use to invoke the cdk?"
    },
    "tags": {
      "type": "string",
      "description": "Add tags to the project (used for linting)",
      "alias": "t"
    },
    "directory": {
      "type": "string",
      "description": "A directory where the project is placed",
      "alias": "d"
    }
  },
  "required": ["name", "cdkCommand"]
}
